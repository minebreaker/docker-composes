services:
  uptime-kuma:
    image: louislam/uptime-kuma:latest
    user: "1000:1000"
    ports:
      - 8083:3001
    environment: [ ]
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    logging:
      driver: local
