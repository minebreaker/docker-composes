services:
  mikochi:
    image: zer0tonin/mikochi:latest-armv7
    volumes:
      - /shared/mikochi:/data
    ports:
      - 8082:8080
    environment:
      USERNAME: test
      PASSWORD: ${PASSWORD}
    user: "1000:1000"
    restart: unless-stopped
    logging:
      driver: local
